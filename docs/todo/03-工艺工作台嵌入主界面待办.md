# 工艺工作台嵌入主界面（待办）

> 状态：**已完成（2026-08-13）**。已将 `MainWindow` 改为 `TabControl`（「导入检查」/「工艺工作台」两个标签页），工作台作为内嵌标签页而非整窗替换。

## 现状

- `MainWindow.EnterWorkbenchAsync()`：读取 DXF 几何后，`Content = new CadWorkbenchView(viewModel)`，整窗切换。
- 用户点「进入工艺工作台」后，看不到导入检查器；无返回/切换入口。

## 目标态

1. 主窗口采用**单一主界面** + 内嵌导航（标签页 / 侧栏 / 分区），导入检查器与工艺工作台是其中的两个视图。
2. 进入工作台不丢失导入上下文（项目、DXF 路径、诊断）。
3. 能随时在导入 / 工作台之间切换。

## 待办项

| 项 | 说明 | 状态 |
|---|---|---|
| 主界面布局改造 | 引入标签页或内嵌分区容器，替代整窗 `Content` 替换 | ⬜ |
| 工作台嵌入 | `CadWorkbenchView` 作为主界面内嵌视图 | ⬜ |
| 状态流转 | 导入 → 工作台 → 返回导入的上下文保持 | ⬜ |
| 画布随窗口尺寸自适应 | 嵌入后画布 fit-to-view 需随容器尺寸变化重算 | ⬜ |

## 关联

- 画布渲染任务：`.trellis/tasks/08-13-workbench-canvas-rendering`（已归档）。
- 工艺工作台：`src/LeatherNesting.Desktop/Views/CadWorkbenchView.cs`。
- 主窗口：`src/LeatherNesting.Desktop/Views/MainWindow.cs`。

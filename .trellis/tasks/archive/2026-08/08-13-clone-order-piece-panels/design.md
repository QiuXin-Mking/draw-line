# Technical Design

Replace left-shell DEMO text with dedicated compact order, piece-card and progress controls backed by one shared deterministic record model. Preserve stable shell hosts. The batch property dialog reuses the same records and maintains image-13 column order; uncertain formulas remain labelled evidence gaps.

Tests assert image-27 card fields/order/dimensions, shared summary updates, property-table columns/defaults, and no independent full-page navigation.

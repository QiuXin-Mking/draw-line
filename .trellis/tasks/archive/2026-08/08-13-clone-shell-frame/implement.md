# Implementation Plan

1. Add failing shell-frame contract tests for five body regions, ratios, ordering, compact styling and image-27 labels.
2. Add evidence-backed classic workstation visual tokens and reusable bordered pane header/host controls.
3. Refactor `AppShellView` to the persistent left/center/right frame while retaining top command routing, module cache and Workspace updates.
4. Provide deterministic initial shell content for screenshot review, clearly marked as demo where values are not real.
5. Run Desktop tests, solution build and `git diff --check`; inspect the 1366×768 frame.

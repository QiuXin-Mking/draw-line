# UI M11 导出交接

## Goal

实现输出选择、映射、命名、目录和 manifest 预览的演示页。

## Requirements

- 只拥有 `Modules/Export/` 与对应测试。
- 显示 DXF、JSON/CSV、PNG/PDF、命名/目录/单位/原点/图层映射和 manifest。
- 文件写入、外部程序、PLT/DWG、设备发送全部为 TODO，禁止执行。

## Acceptance Criteria

- [ ] 阻断校验时生产导出入口显示禁用原因。
- [ ] manifest 字段完整、可读且标 DEMO。
- [ ] 外部程序路径绝不执行。

## Reference

父任务 `implement.md` T11 与产品功能表 M11。

# UI M06 裁片订单

## Goal

实现裁片、尺码、数量和放置约束的高信息密度演示页。

## Requirements

- 只拥有 `Modules/Pieces/` 与对应测试。
- 提供缩略图/表格、搜索、排序、未完成筛选、检查器和计划/已放/未放统计。
- 任何内存编辑均标 TODO；保存、批量写入、真实排样回写均未实现。

## Acceptance Criteria

- [ ] `未放=max(需求-已放,0)` 正确显示。
- [ ] 批量勾选只影响演示选中项。
- [ ] 每个编辑入口显示 TODO。

## Reference

父任务 `implement.md` T06 与产品功能表 M06。

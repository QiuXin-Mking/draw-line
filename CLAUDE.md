# CLAUDE.md

皮革划线排样软件（.NET / Avalonia）。核心职责是排样（nesting），不生成切割刀路（刀路由下游切片软件完成）。

## DXF 输出约定

排样结果导出 DXF 时，用颜色码（ACI 62）区分线角色，与远程排样系统保持一致：

- `0` 外轮廓（Bound）
- `3` 切割线（Cutoff，切穿）
- `5` 标记线 / 刀口（Cutoff，半切/记号）

输出仅含几何线（绝对坐标 + 闭合位），不含刀路。

## 文档组织规范

- **决策记录（ADR）** → 放在 `docs/adr/`，命名统一为数字前缀：`NN-名字.md`（如 `01-dxf-adapter.md`、`02-职责.md`）。
- **任务计划 / 待办** → 放在 `docs/todo/`，同样用数字前缀命名（如 `01-json输出契约待办.md`）。
- 新增文档遵循上述目录与命名约定，不要散落到其他目录（`docs/decisions/` 已废弃，不再使用）。
